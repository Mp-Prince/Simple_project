alert("Thank of a number ");

alert("Duble the number \n means multiply the number with 2");

let randomNumber = Math.floor(Math.random()*100);
let add = "Add the number "+randomNumber;
alert(add);
alert("Divide the number in half");

alert("Subtract the number which u was thanking in the Beggning ");

result = "The Number In ur Mind is : "+randomNumber/2+" ";

alert(result);